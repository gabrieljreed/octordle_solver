[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "octordle_solver_rs"
version = "0.1.0"
description = "Rust-backed solver for Wordle/Octordle puzzles"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
